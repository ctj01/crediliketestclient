import { Box, Button } from '@mui/material'
import React from 'react'

const Header = () => {
  <Box>
      <Button color='primary' variant='contained' disabled={true}>Empleados</Button>
  </Box>
}

export default Header